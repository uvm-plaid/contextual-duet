

-- red let

let main = pλ x : ℕ ⋅ 1.0 , z : ℕ ⋅ 1.0  ⇒
  letp y = (plus₁ x z)
  in return y
in main


-- let main = sλ x : ℕ ⇒
--   let z = plus₁ 1 2 in
--   let y = (plus₁ x z)
--   in y
-- in main
