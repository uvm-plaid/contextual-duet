let apply =  ∀ τ₁ : ☆,
               τ₂ : ☆,
               s  : ℝ⁺
               .

            sλ f : (c : τ₁) ⊸ [c⋅s] τ₂,
               x : τ₁
               ⇒
  f x
in apply @ ℝ @ ℝ @ ℝ⁺[1.0] (sλ x : ℝ ⇒ x) 100.0
